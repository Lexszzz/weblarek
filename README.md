# Проектная работа "Веб-ларек"

Стек: HTML, SCSS, TS, Vite

Структура проекта:
- src/ — исходные файлы проекта
- src/components/ — папка с JS компонентами
- src/components/base/ — папка с базовым кодом

Важные файлы:
- index.html — HTML-файл главной страницы
- src/types/index.ts — файл с типами
- src/main.ts — точка входа приложения
- src/scss/styles.scss — корневой файл стилей
- src/utils/constants.ts — файл с константами
- src/utils/utils.ts — файл с утилитами

## Установка и запуск
Для установки и запуска проекта необходимо выполнить команды

```
npm install
npm run dev
```

или

```
yarn
yarn dev
```
## Сборка

```
npm run build
```

или

```
yarn build
```
# Интернет-магазин «Web-Larеk»
«Web-Larеk» — это интернет-магазин с товарами для веб-разработчиков, где пользователи могут просматривать товары, добавлять их в корзину и оформлять заказы. Сайт предоставляет удобный интерфейс с модальными окнами для просмотра деталей товаров, управления корзиной и выбора способа оплаты, обеспечивая полный цикл покупки с отправкой заказов на сервер.

## Архитектура приложения

Код приложения разделен на слои согласно парадигме MVP (Model-View-Presenter), которая обеспечивает четкое разделение ответственности между классами слоев Model и View. Каждый слой несет свой смысл и ответственность:

Model - слой данных, отвечает за хранение и изменение данных.  
View - слой представления, отвечает за отображение данных на странице.  
Presenter - презентер содержит основную логику приложения и  отвечает за связь представления и данных.

Взаимодействие между классами обеспечивается использованием событийно-ориентированного подхода. Модели и Представления генерируют события при изменении данных или взаимодействии пользователя с приложением, а Презентер обрабатывает эти события используя методы как Моделей, так и Представлений.

### Базовый код

#### Класс Component
Является базовым классом для всех компонентов интерфейса.
Класс является дженериком и принимает в переменной `T` тип данных, которые могут быть переданы в метод `render` для отображения.

Конструктор:  
`constructor(container: HTMLElement)` - принимает ссылку на DOM элемент за отображение, которого он отвечает.

Поля класса:  
`container: HTMLElement` - поле для хранения корневого DOM элемента компонента.

Методы класса:  
`render(data?: Partial<T>): HTMLElement` - Главный метод класса. Он принимает данные, которые необходимо отобразить в интерфейсе, записывает эти данные в поля класса и возвращает ссылку на DOM-элемент. Предполагается, что в классах, которые будут наследоваться от `Component` будут реализованы сеттеры для полей с данными, которые будут вызываться в момент вызова `render` и записывать данные в необходимые DOM элементы.  
`setImage(element: HTMLImageElement, src: string, alt?: string): void` - утилитарный метод для модификации DOM-элементов `<img>`


#### Класс Api
Содержит в себе базовую логику отправки запросов.

Конструктор:  
`constructor(baseUrl: string, options: RequestInit = {})` - В конструктор передается базовый адрес сервера и опциональный объект с заголовками запросов.

Поля класса:  
`baseUrl: string` - базовый адрес сервера  
`options: RequestInit` - объект с заголовками, которые будут использованы для запросов.

Методы:  
`get(uri: string): Promise<object>` - выполняет GET запрос на переданный в параметрах ендпоинт и возвращает промис с объектом, которым ответил сервер  
`post(uri: string, data: object, method: ApiPostMethods = 'POST'): Promise<object>` - принимает объект с данными, которые будут переданы в JSON в теле запроса, и отправляет эти данные на ендпоинт переданный как параметр при вызове метода. По умолчанию выполняется `POST` запрос, но метод запроса может быть переопределен заданием третьего параметра при вызове.  
`handleResponse(response: Response): Promise<object>` - защищенный метод проверяющий ответ сервера на корректность и возвращающий объект с данными полученный от сервера или отклоненный промис, в случае некорректных данных.

#### Класс EventEmitter
Обработчик событий реализует паттерн "Наблюдатель", позволяющий отправлять события и подписываться на события, происходящие в системе. Класс используется для связи слоя данных и представления.

Конструктор класса не принимает параметров.

Поля класса:  
`_events: Map<string | RegExp, Set<Function>>)` -  хранит коллекцию подписок на события. Ключи коллекции - названия событий или регулярное выражение, значения - коллекция функций обработчиков, которые будут вызваны при срабатывании события.

Методы класса:  
`on<T extends object>(event: EventName, callback: (data: T) => void): void` - подписка на событие, принимает название события и функцию обработчик.  
`emit<T extends object>(event: string, data?: T): void` - инициализация события. При вызове события в метод передается название события и объект с данными, который будет использован как аргумент для вызова обработчика.  
`trigger<T extends object>(event: string, context?: Partial<T>): (data: T) => void` - возвращает функцию, при вызове которой инициализируется требуемое в параметрах событие с передачей в него данных из второго параметра.


### Данные
Данные хранятся с помощью двух интерфейсов: товара и  покупателя.

Интерфейс товара (IProduct) описывает структуру данных, связанных с единицей каталога. Он включает в себя уникальный идентификатор, текстовое описание, ссылку на изображение, название товара, категорию товара и его цену. Поле price допускает значение null, что позволяет учитывать товары без установленной цены.

interface IProduct {
  id: string;           // Уникальный идентификатор товара
  description: string;  // Описание товара
  image: string;        // URL изображения товара
  title: string;        // Название товара
  category: string;     // Категория товара
  price: number | null; // Цена (может отсутствовать)
}

Интерфейс покупателя (IBuyer) определяет данные, необходимые для оформления заказа. Он содержит информацию о способе оплаты, а также контактные данные пользователя: электронную почту, телефон и адрес доставки.

interface IBuyer {
  payment: TPayment; // Способ оплаты (тип описывается отдельно)
  email: string;     // Email покупателя
  phone: string;     // Телефон
  address: string;   // Адрес доставки
}

### Модели данных
Для учета данных в приложении используются три класса, разделенные между собой по смыслу и зонам ответственности:

##### Каталог товаров:
хранит следующие данные:

products: LarekProduct[] //массив всех товаров
selectedProduct: LarekProduct | null //выбранный товар или его отсутствие

содержит методы:

setProducts   //сохранение массива товаров полученного в параметрах метода;
getProducts   //получение массива товаров из модели;
getProductById  //получение одного товара по его id;
setSelectedProduct  //сохранение товара для подробного отображения;
getSelectedProduct    //получение товара для подробного отображения.

##### Корзина:
хранит следующие данные:
items: LarekProduct[] //массив товаров, выбранных покупателем для покупки

методы:
getItems  //получение массива товаров, которые находятся в корзине;
addItem   //добавление товара, который был получен в параметре, в массив корзины;
removeItem  //удаление товара, полученного в параметре из массива корзины;
clear   //очистка корзины;
getTotalPrice     //получение стоимости всех товаров в корзине;
getTotalCount     //получение количества товаров в корзине;
hasItem       //проверка наличия товара в корзине по его id, полученного в параметр метода.

##### Покупатель:
хранит следующие данные:
payment: TPayment | null //вид оплаты или его отсутствие
email: string;  //email
phone: string; //телефон
address: string; //адреc

содержит методы:
setData     //сохранение данных в модели
getData     //получение всех данных покупателя
clear     //очистка данных покупателя
validate      //валидация данных

#### Слой коммуникации

В приложении используется отдельный класс, отвечающий за взаимодействие с сервером. Данный класс инкапсулирует работу с API и использует композицию — принимает класс Api через конструктор.

Класс выполняет задачи:
larekProductsResponse //получение списка товаров с сервера (GET)
larekOrderRequest //отправка данных заказа на сервер (POST) 
larekOrderResponse //получение ответа сервера после оформления заказа

Методы класса используют интерфейс IApi и его методы GET и POST, что позволяет разделить сетевую логику и бизнес логику приложения.

#### Классы слоя представления (VIEW)


##### Класс BaseCard
Базовый класс для отображения карточек товаров. Используется как родительский класс для компонентов, работающих с различными шаблонами карточек (каталог, превью, корзина). Класс реализует общий функционал отображения данных товара и обновления DOM-элементов карточки.

###### Тип данных: BaseCardData
Описывает базовые данные, необходимые для отображения карточки товара:

id: string — уникальный идентификатор товара
title: string — название товара
image: string — ссылка на изображение
category: string — категория товара
priceLabel: string — строковое представление цены

###### Наследование
Класс является дженериком:
BaseCard<T extends BaseCardData>

Позволяет дочерним классам расширять базовый тип данных (например, добавлять описание, кнопки и т.д.).
Наследуется от: Component<T>

###### Конструктор: constructor(container: HTMLElement)
Принимает DOM-элемент карточки и находит внутри него необходимые элементы разметки:
.card__title
.card__image
.card__category
.card__price

Ссылки на элементы сохраняются в полях класса.

###### Поля класса
titleElement: HTMLElement — элемент заголовка
imageElement: HTMLImageElement — элемент изображения
categoryElement: HTMLElement — элемент категории
priceElement: HTMLElement — элемент цены
id: string — внутреннее хранение идентификатора товара

###### Сеттеры
Класс использует сеттеры для обновления DOM при вызове метода render:

idValue: string - Сохраняет идентификатор товара внутри компонента
title: string - Обновляет текст заголовка карточки
image: string - Устанавливает изображение через метод setImage
category: string - Устанавливает текст категории, сбрасывает предыдущие CSS-классы, добавляет модификатор класса в зависимости от категории (Для определения класса используется объект categoryMap)
priceLabel: string - Устанавливает текстовое представление цены

###### Дополнительно
BaseCard является частью View-слоя и служит основой для:
ProductCard — карточка в каталоге
ProductPreview — карточка в модальном окне

Общий функционал вынесен в базовый класс, что позволяет избежать дублирования кода в дочерних компонентах.


##### Класс ProductCard
Компонент отображения карточки товара в каталоге (главная страница).
Отвечает за отображение краткой информации о товаре и обработку клика по карточке.

###### Наследование
Класс наследует базовую логику отображения из BaseCard и использует базовый тип данных без расширений:
ProductCard extends BaseCard<BaseCardData>

###### Конструктор:
constructor(container: HTMLElement, events: EventEmitter)

Принимает:
container: HTMLElement — корневой DOM-элемент карточки
events: EventEmitter — триггер для событий для взаимодействия с Presenter

При клике на карточку генерируется событие this.events.emit("product:select", { id: this.id }); в котором:
Генерируемые события:
product:select — пользователь выбрал карточку товара
Данные события:
{ id: string }

###### Дополнительно
Компонент относится к слою View и выполняет две задачи:
- Отображение карточки товара в каталоге
- Уведомление Presenter о выборе товара

Использует BaseCard для отображения данных. Взаимодействует с Presenter через EventEmitter

###### Сценарий логики
product:select → Presenter → Catalog → product:selected → ProductPreview


##### Класс ProductPreview
Компонент отображения информации о товаре. Используется для показа карточки товара в модальном окне.

###### Тип данных
ProductPreviewData
Расширяет базовый тип BaseCardData и содержит дополнительные поля:
description: string — описание товара
buttonText: string — текст кнопки действия
isButtonDisabled: boolean — состояние кнопки (активна/заблокирована)

###### Наследование
ProductPreview extends BaseCard<ProductPreviewData>
Наследует базовую логику отображения карточки и расширяет ее дополнительными элементами.

###### Конструктор
constructor(container: HTMLElement, events: EventEmitter)
Принимает:
container: HTMLElement — корневой DOM-элемент карточки
events: EventEmitter — обработчик событий

В конструкторе:
- вызывается родительский конструктор
- сохраняется ссылка на EventEmitter
- находятся дополнительные элементы:
.card__text — описание
.card__button — кнопка действия
- устанавливается обработчик клика по кнопке

###### Поля класса:
descriptionElement: HTMLElement — элемент описания
buttonElement: HTMLButtonElement — кнопка действия

###### Сеттеры
description: string - Устанавливает текст описания товара
buttonText: string - Устанавливает текст кнопки
isButtonDisabled: boolean - Управляет состоянием кнопки:
true — кнопка заблокирована
false — кнопка активна

###### Обработка событий
При клике на кнопку генерируется событие this.events.emit("product:toggle", { id: this.id }); в котором:
Генерируемые события:
product:toggle — изменение состояния товара в корзине
Данные события:
{ id: string }

###### Дополнительно
Компонент относится к слою View и выполняет:
- Отображение полной информации о товаре
- Отправку события при взаимодействии пользователя

Связь с другими компонентами:
- Использует BaseCard как основу
- Отображается внутри Modal
- Взаимодействует с Presenter через EventEmitter

###### Сценарий логики:
product:selected → Presenter → ProductPreview → product:toggle → Presenter → Cart


##### Класс CartItem
Компонент отображения одного товара в списке корзины. Используется внутри компонента CartView для формирования списка выбранных товаров.

###### Тип данных
CartItemData
Описывает данные, необходимые для отображения элемента корзины:
id: string — идентификатор товара
title: string — название товара
priceLabel: string — отображаемая цена
index: number — порядковый номер в списке

###### Наследование
CartItem extends Component<CartItemData>
Компонент не использует BaseCard, так как имеет отдельную компактную разметку.

###### Конструктор
constructor(container: HTMLElement, events: EventEmitter)

Принимает:
container: HTMLElement — DOM-элемент элемента списка
events: EventEmitter — обработчик событий

В конструкторе:
- Сохраняется ссылка на EventEmitter
- Находятся элементы:
.basket__item-index — номер элемента
.card__title — название
.card__price — цена
.basket__item-delete — кнопка удаления
- Устанавливается обработчик клика по кнопке удаления

###### Поля класса
indexElement: HTMLElement — номер позиции
titleElement: HTMLElement — название товара
priceElement: HTMLElement — цена
deleteButton: HTMLButtonElement — кнопка удаления
id: string — идентификатор товара

###### Сеттеры
index: number - Устанавливает порядковый номер товара в списке
title: string - Обновляет название товара
priceLabel: string - Устанавливает текст цены
idValue: string - Сохраняет идентификатор товара

###### Обработка событий
При клике на кнопку удаления генерируется событие this.events.emit("cart:remove", { id: this.id }); в котором:
Генерируемые события
cart:remove — удаление товара из корзины
Данные события:
{ id: string }

###### Дополнительно
Компонент относится к слою View и выполняет:
- Отображение одного элемента корзины
- Уведомление Presenter о намерении удалить товар

Связь с другими компонентами:
- используется внутри CartView
- взаимодействует с Presenter через EventEmitter

###### Сценарий логики:
cart:remove → Presenter → Cart → cart:changed


##### Класс CartView
Компонент отображения корзины пользователя.
Отвечает за отображение списка товаров, общей стоимости и кнопки оформления заказа.

###### Тип данных
CartViewData
Описывает данные, необходимые для отображения корзины:
items: HTMLElement[] — массив элементов списка (CartItem)
totalLabel: string — строковое представление общей стоимости
isCheckoutDisabled: boolean — состояние кнопки оформления

###### Наследование
CartView extends Component<CartViewData>

###### Конструктор
constructor(container: HTMLElement, events: EventEmitter)
Принимает:
container: HTMLElement — DOM-элемент корзины
events: EventEmitter — обработчик событий

В конструкторе:
сохраняется ссылка на EventEmitter
находятся элементы:
.basket__list — список товаров
.basket__price — общая стоимость
.basket__button — кнопка оформления
устанавливается обработчик клика по кнопке оформления

###### Поля класса
listElement: HTMLElement — контейнер списка
priceElement: HTMLElement — элемент общей стоимости
checkoutButton: HTMLButtonElement — кнопка оформления

###### Сеттеры
items: HTMLElement[] - очищает список, вставляет элементы товаров, отображение сообщения «Корзина пуста»
totalLabel: string- Устанавливает текст общей стоимости
isCheckoutDisabled: boolean - Управляет состоянием кнопки (true — кнопка отключена, false — активна)

###### Обработка событий
При клике на кнопку оформления генерируется событие this.events.emit("order:start"); в котором:
Генерируемые события
order:start — пользователь начинает оформление заказа

###### Дополнительно 
Компонент относится к слою View и выполняет:
- Отображение состояния корзины
- Инициацию оформления заказа

Связь с другими компонентами:
- Использует CartItem для отображения элементов
- Отображается внутри Modal
- Взаимодействует с Presenter через события

###### Сценарий логики:
basket:open → Presenter → CartView → order:start → Presenter → OrderForm


##### Класс Header
Компонент отображения шапки сайта.
Отвечает за отображение счетчика товаров в корзине и обработку открытия корзины.

###### Тип данных
HeaderData
Описывает данные, необходимые для отображения:
counter: number — количество товаров в корзине

###### Наследование
Header extends Component<HeaderData>

###### Конструктор
constructor(container: HTMLElement, events: EventEmitter)
Принимает:
container: HTMLElement — DOM-элемент шапки
events: EventEmitter — обработчик событий
В конструкторе:
сохраняется ссылка на EventEmitter
находятся элементы:
.header__basket — кнопка корзины
.header__basket-counter — счетчик
устанавливается обработчик клика по кнопке корзины

###### Поля класса
basketButton: HTMLButtonElement — кнопка открытия корзины
counterElement: HTMLElement — элемент счетчика

###### Сеттеры
counter: number - Устанавливает значение счетчика товаров в корзине

###### Обработка событий
При клике на кнопку корзины генерируется событие: this.events.emit("basket:open"); в котором:
Генерируемые события
basket:open — пользователь открыл корзину

###### Дополнительно
Компонент относится к слою View и выполняет:
- Отображение состояния корзины (количество товаров)
- Уведомление о намерении пользователя открыть корзину

###### Связь с другими компонентами
Обновляется при событии cart:changed
Взаимодействует с Presenter через EventEmitter

###### Сценарии логики:
cart:changed → Presenter → Header.render()
basket:open → Presenter → CartView


##### Класс Modal
Компонент управления модальным окном.
Отвечает за отображение, скрытие и подстановку контента в модальное окно.

###### Наследование
Modal extends Component<unknown>

###### Типы данных
Тип данных не используется, так как компонент не работает с остальной структурой данных, а отображает произвольный контент.

###### Конструктор
constructor(container: HTMLElement, events: EventEmitter)

Принимает:
container: HTMLElement — корневой DOM-элемент модального окна
events: EventEmitter — обработчик событий

В конструкторе:
сохраняется ссылка на EventEmitter
находятся элементы:
.modal__content — контейнер содержимого
.modal__close — кнопка закрытия
устанавливаются обработчики:
клик по кнопке закрытия
клик вне контента (на оверлей)

###### Поля класса
contentElement: HTMLElement — контейнер для отображаемого контента
closeButton: HTMLButtonElement — кнопка закрытия

###### Методы
setContent(content: HTMLElement): void - Заменяет текущее содержимое модального окна на переданный DOM-элемент, очищает предыдущий контент и вставляет новый
open(): void - Открывает модальное окно, добавляет класс modal_active, делает окно видимым
close(): void - Закрывает модальное окно, удаляет класс modal_active, очищает содержимое, генерирует событие закрытия

Генерируемые события
modal:close — модальное окно закрыто

###### Обработка событий
Компонент обрабатывает:
- клик по кнопке закрытия
- клик по области вне контента
В обоих случаях вызывается метод close().

###### Дополнительно
Компонент относится к слою View и выполняет:
- Управление отображением модального окна
- Контейнеризацию других View-компонентов

Модальное окно:
- не имеет дочерних классов
- не наследуется другими компонентами
- не содержит логики конкретных экранов
Все компоненты, отображаемые внутри него, являются независимыми.

###### Связь с другими компонентами
принимает в setContent любые View-компоненты
используется Presenter для отображения:
ProductPreview
CartView
OrderForm
ContactsForm
Success

###### Сценарии логики
Presenter → modal.setContent(component)
Presenter → modal.open()
клик вне окна / крестик
→ Modal.close()
→ emit("modal:close")


##### Класс BaseForm
Базовый класс для компонентов форм.
Используется как родительский для всех форм в приложении (оформление заказа и ввод контактов).
Реализует общий функционал обработки ввода, валидации и отправки формы.

###### Тип данных
BaseFormData
Описывает общее состояние формы:
isValid: boolean — валидность формы
errors: string — текст ошибки

###### Наследование
BaseForm<T extends Record<string, any>> extends Component<T>
Класс является обобщенным и может работать с различными структурами данных форм.

###### Конструктор
constructor(container: HTMLFormElement, events: EventEmitter)

Принимает:
container: HTMLFormElement — DOM-элемент формы
events: EventEmitter — обработчик событий

В конструкторе:
- Сохраняется ссылка на EventEmitter
- Присутствуют элементы:
.form__errors (блок отображения ошибок)
кнопка отправки (button[type="submit"])
- Устанавливаются обработчики:
input — изменение полей
submit — отправка формы

###### Поля класса
formElement: HTMLFormElement — корневой элемент формы
submitButton: HTMLButtonElement — кнопка отправки
errorsElement: HTMLElement — блок ошибок

###### Методы
getFormData(): Record<string, string>

Собирает данные формы:
проходит по полям формы
формирует объект вида { name: value }

###### Сеттеры
isValid: boolean

Управляет состоянием кнопки отправки:
true — кнопка активна
false — отключена
errors: string
Устанавливает текст ошибок в соответствующий блок.

###### Обработка событий
Изменение формы
При вводе данных генерируется событие this.events.emit("form:change", formData) в котором:
formData — текущие значения полей формы

Отправка формы
При отправке формы предотвращается стандартное поведение (preventDefault) и генерируется событие this.events.emit("form:submit", formData) в котором:
form:change — изменение данных формы
form:submit — отправка формы

###### Дополнительно
Компонент относится к слою View и выполняет:
Сбор пользовательского ввода
Отображение состояния формы
Генерацию событий для Presenter

###### Связь с другими компонентами
используется как базовый класс для:
OrderForm
ContactsForm
взаимодействует с Presenter через события

###### Сценарии логики:
ввод данных → form:change → Presenter → Buyer.validate → обновление формы
submit → form:submit → Presenter → следующий шаг / отправка заказа

##### Класс OrderForm
Компонент формы первого шага оформления заказа. Отвечает за выбор способа оплаты и ввод адреса доставки.

###### Тип данных
OrderFormData
Описывает данные формы:
address: string — адрес доставки
payment: string — способ оплаты (при получении или онлайн)

###### Наследование
OrderForm extends BaseForm<OrderFormData>
Наследует общий функционал работы с формами из BaseForm.

###### Конструктор
constructor(container: HTMLFormElement, events: EventEmitter)
Принимает:
container: HTMLFormElement — DOM-элемент формы
events: EventEmitter — обработчик событий

В конструкторе:
- вызывается родительский конструктор
- находятся элементы:
кнопки выбора оплаты (button[name="card"], button[name="cash"])
- устанавливаются обработчики клика по кнопкам

###### Поля класса
paymentButtons: HTMLButtonElement[] — список кнопок выбора оплаты

###### Сеттеры
address: string - Устанавливает значение поля адреса
payment: string - Обновляет выбранный способ оплаты, управляет визуальным состоянием кнопок, добавляет класс button_alt-active для выбранной кнопки, 
убирает у остальных

###### Обработка событий
Выбор способа оплаты:
При клике на кнопку оплаты - обарабатывается с помощью this.events.emit("form:change", {payment: выбранное_значение});

Ввод адреса - Обрабатывается в BaseForm через событие input 
this.events.emit("form:change", formData);

Отправка формы - Обрабатывается в BaseForm
this.events.emit("form:submit", formData);

###### Генерируемые события
form:change — изменение данных формы
form:submit — переход ко второму шагу оформления


###### Дополнительно
Компонент относится к слою View и выполняет:
- Сбор данных первого шага оформления
- Отображение состояния формы
- Генерацию событий для Presenter

###### Связь с другими компонентами
Васледует поведение BaseForm
Отображается внутри Modal
Взаимодействует с Presenter

###### Сценарии логики:
order:start → Presenter → OrderForm
form:change → Presenter → Buyer.validate
submit → form:submit → Presenter → ContactsForm


##### Класс ContactsForm
Компонент формы ввода контактных данных покупателя (второй шаг оформления).
Отвечает за ввод email и телефона и завершение оформления заказа.

###### Тип данных
ContactsFormData

Описывает данные формы:
email: string — электронная почта
phone: string — номер телефона

###### Наследование
ContactsForm extends BaseForm<ContactsFormData>
Наследует общий функционал работы с формами из BaseForm.

###### Конструктор
constructor(container: HTMLFormElement, events: EventEmitter)

Принимает:
container: HTMLFormElement — DOM-элемент формы
events: EventEmitter — обработчик событий

В конструкторе:
вызывается родительский конструктор

###### Сеттеры
email: string - Устанавливает значение поля email
phone: string - Устанавливает значение поля телефона

###### Обработка событий
Ввод данных - Обрабатывается в BaseForm:
this.events.emit("form:change", formData);

Отправка формы - Обрабатывается в BaseForm:
this.events.emit("form:submit", formData);

###### Генерируемые события
form:change — изменение данных формы
form:submit — завершение оформления заказа

###### Назначение в архитектуре

Компонент относится к слою View и выполняет:
- Сбор контактных данных
- Отображение состояния формы
- Инициацию завершения заказа

###### Связь с другими компонентами
Наследует поведение BaseForm
Отображается внутри Modal
Взаимодействует с Presenter

###### Сценарии логики:
ContactsForm → form:submit → Presenter → API → Success

##### Класс Success
Компонент отображения успешного оформления заказа.
Отвечает за показ сообщения об успешной покупке и итоговой суммы списания.

###### Тип данных
SuccessData - описывает данные, необходимые для отображения
totalLabel: string — строковое представление суммы заказа

###### Наследование
Success extends Component<SuccessData>

###### Конструктор
constructor(container: HTMLElement, events: EventEmitter)

Принимает:
container: HTMLElement — DOM-элемент компонента
events: EventEmitter — обработчик событий

В конструкторе:
сохраняется ссылка на EventEmitter
находятся элементы:
.order-success__description — текст с суммой
.order-success__close — кнопка закрытия
устанавливается обработчик клика по кнопке

###### Поля класса
descriptionElement: HTMLElement — элемент описания
closeButton: HTMLButtonElement — кнопка закрытия

###### Сеттеры
totalLabel: string - Устанавливает текст с суммой списания.

При клике на кнопку генерируется событие this.events.emit("success:close"); в котором:
success:close — пользователь завершил сценарий покупки

###### Назначение в архитектуре
Компонент относится к слою View и выполняет:
- Отображение результата оформления заказа
- Завершение пользовательского сценария

###### Связь с другими компонентами
отображается внутри Modal
взаимодействует с Presenter через событие

###### Сценарии логики:
API → Presenter → Success → success:close → Presenter → modal.close()


#### События приложения
В приложении используется событийно-ориентированный подход.
Все взаимодействия между слоями происходят через EventEmitter.

Ниже приведен список событий, генерируемых в системе:

##### События моделей данных
catalog:changed - Изменение списка товаров в каталоге
Источник: Catalog, вызывается при загрузке или обновлении списка товаров

product:selected - Изменение выбранного товара
Источник: Catalog, вызывается при выборе товара для просмотра

cart:changed - Изменение содержимого корзины
Источник: Cart, вызывается при добавлении или удалении товара, очистке корзины

buyer:changed -Изменение данных покупателя
Источник: Buyer, вызывается при обновлении данных формы

##### События представления (View)
product:select - Выбор карточки товара
Источник: ProductCard
Данные:
{ id: string }

product:toggle - Добавление или удаление товара из корзины
Источник: ProductPreview
Данные:
{ id: string }

basket:open - Открытие корзины
Источник: Header

cart:remove - Удаление товара из корзины
Источник: CartItem
Данные:
{ id: string }

order:start - Начало оформления заказа
Источник: CartView

form:change - Изменение данных формы
Источник: BaseForm
Данные:
Record<string, string>

form:submit - Отправка формы
Источник: BaseForm
Данные:
Record<string, string>

success:close - Закрытие окна успешного оформления заказа
Источник: Success

modal:close - Закрытие модального окна
Источник: Modal

##### Дополнительно
События генерируются только слоями Model и View.
Presenter не генерирует события, а только обрабатывает их.
Все решения о логике приложения обрабатываются в Presenter.